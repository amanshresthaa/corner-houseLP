# Implementation Checklist

## Prep
- [x] Refresh design plan to include new CTA cluster and heading.
- [x] Sketch revised section structure in `app/menu/page.tsx`.

## Layout & Content
- [x] Configure left column with `RestaurantHoursCard` (`variant="light"`) inside new card container.
- [x] Build right column with “Restaurant & Bar Opening Time” heading, supporting copy, and quick fact highlights.
- [x] Implement CTA cluster: `Book now` (primary), `Call now` (secondary), `Get directions` (outline).

## Cleanup
- [x] Confirm contrast on buttons/cards meets AA.
- [x] Ensure there are no duplicate CTAs and focus states are visible on light background.

## Verification
- [x] Run Chrome DevTools manual QA (mobile, tablet, desktop) and capture findings in `verification.md`.

## Questions/Blockers
- None.
