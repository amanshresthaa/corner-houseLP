# Implementation Checklist

## Prep
- [x] Import reusable CTA component into `app/menu/page.tsx`
- [x] Define headline/description/button config for menu CTA

## Implementation
- [x] Insert new `FadeIn`-wrapped `CallToActionSection` between dietary info block and footer
- [x] Ensure styling/class overrides keep background light and text dark

## Testing & QA
- [x] Run relevant lint/build command if feasible (e.g. `npm run lint`) *(fails on pre-existing lint issues)*
- [x] Manual verification in browser (Chrome DevTools MCP)
- [x] Update `verification.md` with findings

## Documentation
- [x] Record verification steps & outcomes
